'use client';

export default function Page() {
  // simulate loading time for 6 seconds
  // await new Promise((resolve) => setTimeout(resolve, 6000));

  return (
    <div>
      <h4>DASHBOARD COMPONENT LOADED</h4>
    </div>
  );
}
